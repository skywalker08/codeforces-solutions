#include <bits/stdc++.h>

using namespace std;

int main() {
    int n, k, cnt=0, a, ac=1, bc=1, b, ka, kb, pcnt=0, ans;
    cin >> n >> k;
    int pk=k;
    string str;
    cin >> str;
    int i=0;
    int j=1;
    while (i!=j) {
        while (j<n) {
            
        }
        
    }
    
}